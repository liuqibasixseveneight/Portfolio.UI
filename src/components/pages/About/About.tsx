import { AboutWrapper } from './About.styles';
import type { AboutProps } from './types';

export const About = ({}: AboutProps) => {
  return (
    <AboutWrapper>
      <div>About</div>
    </AboutWrapper>
  );
};
